import type { OderaDocsConfig } from '@oderadocs/cli'

/**
 * Defines the OderaDocs configuration.
 * @param configInput The configuration for OderaDocs.
 * @returns The provided configuration object.
 */
export function defineOderaDocsConfig(configInput: OderaDocsConfig) {
  return configInput
}
