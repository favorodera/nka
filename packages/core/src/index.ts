import type { NkaConfig } from '@nka/cli'

/**
 * Defines the Nka configuration.
 * @param configInput The configuration for Nka.
 * @returns The provided configuration object.
 */
export function defineNkaConfig(configInput: NkaConfig) {
  return configInput
}
